server:
  port: 8084

spring:
  application:
    name: note-service

  datasource:
    url: jdbc:mysql://localhost:3306/devflow_note
    username: root
    password: mona2001
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

jwt:
  secret: YourVeryLongSecretKeyForDevFlowAuthenticationProject123456789                        
  expiration: 3600000